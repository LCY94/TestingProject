<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taruc.androidproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="TARUC">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cardview" android:label="TARUC"></activity>
        <activity android:name=".rview.rviewMain" android:label="TARUC"></activity>
        <activity android:name=".Information" android:label="TARUC"></activity>
        <activity android:name=".pro" android:label="TARUC"  android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".rviewContact" android:label="TARUC"  ></activity>
    </application>

</manifest>